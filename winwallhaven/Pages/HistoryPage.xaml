<?xml version="1.0" encoding="utf-8"?>

<Page
    x:Class="winwallhaven.Pages.HistoryPage"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CommandBar DefaultLabelPosition="Right">
            <CommandBar.Content>
                <StackPanel Orientation="Horizontal" Spacing="8" />
            </CommandBar.Content>
            <CommandBar.PrimaryCommands>
                <AppBarButton Icon="Refresh" Label="Refresh" Command="{Binding RefreshCommand}" />
                <AppBarSeparator />
                <AppBarButton Label="First" Command="{Binding FirstPageCommand}">
                    <AppBarButton.Icon>
                        <PathIcon Data="M6,4 L6,28 9,28 9,4 Z M12,16 L28,4 28,28 Z" />
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton Icon="Previous" Label="Prev" Command="{Binding PrevPageCommand}" />
                <AppBarElementContainer VerticalAlignment="Center">
                    <Grid Height="40" Margin="8,0">
                        <TextBlock Text="{Binding CurrentPage}"
                                   VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Grid>
                </AppBarElementContainer>
                <AppBarButton Icon="Next" Label="Next" Command="{Binding NextPageCommand}" />
                <AppBarSeparator />
                <AppBarButton Icon="Delete" Label="Clear History" Command="{Binding ClearHistoryCommand}" />
            </CommandBar.PrimaryCommands>
        </CommandBar>

        <Grid Grid.Row="1">
            <GridView ItemsSource="{Binding Results}"
                      Style="{StaticResource ResponsiveWallpaperGridViewWithHistoryMenu}" />
        </Grid>

        <!-- Page Loading Overlay -->
        <Grid Grid.RowSpan="2" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}" Opacity="0.85"
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
                <ProgressRing IsActive="True" Width="64" Height="64" />
                <TextBlock Text="Loading history..." FontSize="14" FontWeight="SemiBold" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>